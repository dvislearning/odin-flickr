<h1 id="front_header">Flickr ID Picture Search</h1><br/ >

<%= render 'search_form' %>


<% unless @photos.nil? %>
	<% if @photos == :invalid %>
		<%= render 'invalid' %> 	
	<% elsif @photos == :empty %>
		<%= render 'empty' %> 
	<% else %>
		<%= render 'photos' %> 
	<% end %>
<% end %>